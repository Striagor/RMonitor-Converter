import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "RMonitor Converter - TCP/WebSocket Data Converter",
  description: "Convert data between RMonitor TCP protocol and WebJSON WebSocket. Manage connections, roles, and real-time data streaming.",
  keywords: ["RMonitor", "WebSocket", "TCP", "Converter", "Racing", "Telemetry", "Real-time"],
  authors: [{ name: "RMonitor Converter Team" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
